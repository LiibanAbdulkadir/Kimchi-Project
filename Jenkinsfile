pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/LiibanAbdulkadir/Kimchi-Project.git'
            }
        }
        stage('Robot'){
        
        }
    }
}
